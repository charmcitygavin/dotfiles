- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc: zsh/.zshrc
    ~/.oh-my-zsh: zsh/oh-my-zsh

- shell:
    - [git submodule update --init --recursive, Installing submodules]
